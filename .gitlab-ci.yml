stages:
  - build
  - test
  - deploy
  - publish

publish:
  stage: publish
  image: node:22
  rules:
    - if: '$CI_COMMIT_TAG'
      when: manual
  script:
    - npm ci
    - echo "//registry.npmjs.org/:_authToken=${NPM_KEY}" > .npmrc
    - npm publish